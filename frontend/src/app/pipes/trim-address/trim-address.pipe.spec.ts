import { TrimAddressPipe } from './trim-address.pipe';

describe('TrimAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
